# --- Núcleo (Fase 1) ---
# Versões fixadas para reprodutibilidade (testadas com Python 3.14).
langchain==1.3.9
langchain-ollama==1.1.0
langchain-community==0.4.2
rich==15.0.0

# --- Voz (Fase 2) ---
# STT (Whisper via CTranslate2) e áudio. O Piper (TTS) é um binário externo,
# instalado pelo sistema/AUR — veja o README.
# sounddevice/soundfile exigem PortAudio e libsndfile no sistema:
#   sudo pacman -S portaudio libsndfile
faster-whisper
sounddevice
soundfile
